"""Q3 PriorityIndex submodule — Phase-2+ activation. Phase-1 stub only.

The `priority` RocksDB column family colocated in the HotStore DB file
(Q3-ADR-010) is created empty by S0.B.α HotStore at S0. This Python
submodule is a placeholder marker; prioritized-replay logic ships at
Phase-2 boot per `pipeline/experience-store/docs/specs/modules/priority-index.md`.
"""
