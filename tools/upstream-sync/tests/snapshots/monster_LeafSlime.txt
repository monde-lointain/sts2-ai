You are an engineer subagent in an isolated git worktree for **sts2-ai Wave 5, Stream B.2** — Monster port: `src/Core/Models/Monsters/LeafSlime.cs`.

# PRE-FLIGHT
Expected main SHA: `274beca9d2dd3307248575f0adefa90bf270746c`.
```
git rev-parse HEAD && git fetch origin && git merge --ff-only main
```
HEAD mismatch → STOP, return DONE_WITH_CONCERNS.

# ABSOLUTE-PATH MANDATE
All Edit/Write `file_path` absolute (use `pwd` at session start and keep it as `WROOT`).

# Context
- Upstream version: `v0.105.1`
- Port-decision row:
  - Path: `src/Core/Models/Monsters/LeafSlime.cs`
  - Diff status: `PENDING`
  - Line delta: 120
  - Decision: `PORT`
  - Rationale: New file in monsters

## ⚠ Patch-notes hint (verify against diff)

- **Change type**: added
- **Version**: Beta Hotfix Notes - v0.105.1
- **Excerpt**: "New slime variant"
- **Source**: Steam patch note gid=42

**REQUIRED**: cross-check this hint against the upstream code diff. If hint claims a stat change (e.g., "+2 → +3") that does NOT appear in the .cs diff, document the discrepancy in your port-log row as `patch-notes-claim-vs-code-diff-mismatch`. **Do NOT skip a row just because the diff looks like Godot threading — the hint trumps pattern recognition.**


---
- Upstream file (decompiled): `<UPSTREAM_TREE>/src/Core/Models/Monsters/LeafSlime.cs`
- Target C# file (sts2-ai): `<WROOT>/engine/headless/src/<MIRRORED_PATH>`

# Goal
Port the upstream monster from `src/Core/Models/Monsters/LeafSlime.cs` into the sts2-ai engine.

Concrete tasks:
1. Read the upstream decompiled `.cs` file.
2. Identify the monster class: stats, move set, action logic, intent calculation.
3. Locate or create the corresponding file in `engine/headless/src/`.
4. Implement the port: class scaffold, stats dataclass, moves, `GenerateMonsters(Rng)` override if the monster uses encounter-RNG spawning.
5. Register the monster in the relevant encounter(s) under `engine/headless/src/Core/Models/Encounters/`.
6. Wire the monster into the content registry advisory if applicable (Q4 advisory: `None`).
7. Write a gtest in `engine/headless/test/` covering at minimum: monster can be instantiated; stat block round-trips; first move is non-null.

# Files OWNED
- `<WROOT>/engine/headless/src/Core/Models/Monsters/<MonsterClass>.cs` (NEW or MODIFY)
- `<WROOT>/engine/headless/test/<MonsterClass>Tests.cs` (NEW)
- Any encounter file updated to reference this monster

# Files FORBIDDEN
- `<WROOT>/tools/upstream-sync/**`
- `<WROOT>/.github/workflows/**`
- `<WROOT>/.claude/**`
- `<WROOT>/contracts/schemas/*.proto` — schema edits are a coordination event; surface to project-lead

# Re-surface triggers
- Upstream monster uses a mechanic (orbs, powers, relics) not yet implemented in sts2-ai → DEFER, surface with note
- Upstream file is empty or stub-only → NO_ACTION_NEEDED, surface
- LOC >400 total → surface

# Verification
```bash
cd <WROOT>
# Build
dotnet build engine/headless/Sts2Headless.sln
# Tests
dotnet test engine/headless/Sts2Headless.Tests/ --filter "FullyQualifiedName~<MonsterClass>"
```
All tests must pass before commit.

# Deliverable
Single commit:
```
feat(q1): port LeafSlime.cs — v0.105.1

Wave 5 / Stream B.2.
- LeafSlime.cs: <1-line summary of monster>
- Encounter wiring: <encounter file(s)>
- Tests: <test file>
```

# Reporting
```
Worktree CWD: <pwd>
Branch: worktree-agent-<id>
Preflight SHA: <must be 274beca...>
Commits: <list>
Files changed: <list>
Tests: dotnet test result count
Deferrals: <list or none>
```

Stop. Do NOT merge.
