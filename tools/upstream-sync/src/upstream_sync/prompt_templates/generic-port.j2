You are an engineer subagent in an isolated git worktree for **sts2-ai Wave {{ wave }}, Stream {{ stream_id }}** — {{ bucket | title }} port: `{{ row.path }}`.

# PRE-FLIGHT
Expected main SHA: `{{ expected_sha }}`.
```
git rev-parse HEAD && git fetch origin && git merge --ff-only main
```
HEAD mismatch → STOP, return DONE_WITH_CONCERNS.

# ABSOLUTE-PATH MANDATE
All Edit/Write `file_path` absolute (use `pwd` at session start and keep it as `WROOT`).

# Context
- Upstream version: `{{ version }}`
- Bucket: `{{ bucket }}`
- Port-decision row:
  - Path: `{{ row.path }}`
  - Diff status: `{{ row.status }}`{% if row.line_delta is not none %}
  - Line delta: {{ row.line_delta }}{% endif %}
  - Decision: `{{ row.decision }}`
  - Rationale: {{ row.rationale }}{% if row.re_eval_trigger %}
  - Re-eval trigger: {{ row.re_eval_trigger }}{% endif %}{% if row.character_tag %}
  - Character tag: {{ row.character_tag }}{% endif %}

## ⚠ Patch-notes hint (verify against diff)
{% if row.patch_notes_hint %}
- **Change type**: {{ row.patch_notes_hint.change_type }}{% if row.patch_notes_hint.magnitude %} ({{ row.patch_notes_hint.magnitude }}){% endif %}
- **Version**: {{ row.patch_notes_hint.version }}
- **Excerpt**: "{{ row.patch_notes_hint.excerpt }}"
- **Source**: Steam patch note gid={{ row.patch_notes_hint.gid }}

**REQUIRED**: cross-check this hint against the upstream code diff. If hint claims a stat change (e.g., "+2 → +3") that does NOT appear in the .cs diff, document the discrepancy in your port-log row as `patch-notes-claim-vs-code-diff-mismatch`. **Do NOT skip a row just because the diff looks like Godot threading — the hint trumps pattern recognition.**
{% else %}
- _No patch-notes hint surfaced for this row. Verify diff substance per standard workflow._
{% endif %}

---
- Upstream file (decompiled): `<UPSTREAM_TREE>/{{ row.path }}`
- Target location in sts2-ai: **[DISPATCHER: fill in target path]**

# Goal
Port `{{ row.path }}` from upstream v{{ version }} into sts2-ai.

[DISPATCHER: fill in specific implementation steps for this bucket/file]

Minimum tasks for any port:
1. Read the upstream decompiled `.cs` file.
2. Understand the diff (status: `{{ row.status }}`).
3. Implement or update the corresponding file in sts2-ai.
4. Write or update tests covering the ported logic.

# Files OWNED
[DISPATCHER: list owned files]

# Files FORBIDDEN
- `<WROOT>/tools/upstream-sync/**`
- `<WROOT>/.github/workflows/**`
- `<WROOT>/.claude/**`
- `<WROOT>/contracts/schemas/*.proto` — surface to project-lead before touching

# Re-surface triggers
- Logic depends on a mechanic not yet implemented → DEFER, surface with note
- Upstream file is empty or stub → NO_ACTION_NEEDED, surface
- LOC >400 total → surface

# Verification
[DISPATCHER: fill in verification commands]

# Deliverable
Single commit:
```
feat(<quantum>): port {{ row.path | basename }} — {{ version }}

Wave {{ wave }} / Stream {{ stream_id }}.
[DISPATCHER: fill in commit body]
```

# Reporting
```
Worktree CWD: <pwd>
Branch: worktree-agent-<id>
Preflight SHA: <must be {{ expected_sha[:7] }}...>
Commits: <list>
Files changed: <list>
Tests: <result>
Deferrals: <list or none>
```

Stop. Do NOT merge.
