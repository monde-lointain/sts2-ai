{# Jinja2 template stub. Full content lands in W5 alongside port_decisions.py. #}
{# Variables expected (W5 will refine this contract):
    version_range:    str          e.g. "v0.103.2 → v0.105.1"
    from_buildid:     str
    to_buildid:       str
    generated_at:     str          ISO timestamp
    tool_version:     str          from importlib.metadata
    unmatched_paths:  list[(path, size_bytes, first_seen)]
    summary:          dict[bucket -> count]
    sections:         list[Section] with rows
    q4_advisory:      Q4Advisory
    downstream_regen: list[str]
#}
# Port Decisions — {{ version_range }}

_Generated by `tools/upstream-sync` v{{ tool_version }} at {{ generated_at }}._

Steam buildid: {{ from_buildid }} → {{ to_buildid }}.

{# Full template body — section ordering, per-row columns, Q4 advisory,
   downstream-regen footer — written in W5. This stub exists so test fixtures
   can reference the template path. #}
